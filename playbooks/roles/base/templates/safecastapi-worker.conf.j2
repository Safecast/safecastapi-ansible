start on runlevel [2345]
stop on runlevel [06]

setuid safecast
setgid safecast

respawn

chdir /home/safecast/safecastapi

script
  export RAILS_ENV=development
  exec /usr/local/rvm/wrappers/safecastapi/bundle exec rake jobs:work
end script
